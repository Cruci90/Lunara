{
  "name": "blw-tracker",
  "version": "1.0.0",
  "description": "Baby Led Weaning tracker — seguimiento de alimentación complementaria",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"node --env-file=.env server/index.js\"",
    "dev:client": "vite",
    "dev:server": "node --env-file=.env server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "NODE_ENV=production node --env-file=.env server/index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "express": "^4.21.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.1.0",
    "vite": "^5.4.0"
  }
}
